
&НаСервере
Процедура ПолеПриИзмененииНаСервере()
	
	Объект.Записи.Очистить();
	
	Для Каждого Реквизит Из Объект.Поле.Метаданные().Реквизиты Цикл
		
		НоваяСтрока = Объект.Записи.Добавить();
		НоваяСтрока.ИмяРеквизита = Реквизит.Имя;
		НоваяСтрока.ЗначениеРеквизита = Объект.Поле[Реквизит.Имя];
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолеПриИзменении(Элемент)
	ПолеПриИзмененииНаСервере();
КонецПроцедуры

